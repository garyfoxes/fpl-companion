#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"

echo "Running pre-push checks: yaml + format + lint + unit tests"
npm run check:yaml
npm run format
npm run lint
npm run test
